import { SuiteShape, TestShape } from '@seleniumhq/side-model';
import { Configuration, Project } from './types';
export interface HoistedThings {
    configuration: Configuration;
    logger: Console;
}
declare const buildRunners: ({ configuration, logger }: HoistedThings) => {
    all: (projects: Project[]) => Promise<void>;
    project: (project: Project) => Promise<void>;
    suite: (project: Project, suite: SuiteShape) => Promise<void>;
    test: (project: Project, test: TestShape) => Promise<void>;
};
export default buildRunners;
//# sourceMappingURL=run.d.ts.map